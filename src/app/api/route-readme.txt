Place API routes in nested folders under /src/app/api.
Use Prisma via `import { prisma } from '@/lib/db'`.
All environment variables should be set in .env.local (not committed).
See docs/deployment-guide.md → Step 2 for database setup.
